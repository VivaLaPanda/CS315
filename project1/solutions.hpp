
#ifndef __MYSOLUTION_HPP
#define __MYSOLUTION_HPP

#include<iostream>
#include"reclists.hpp"

list reverse( list p );
list append( list p, list q );
list last( list p );
bool is_lat(list p);

#endif
